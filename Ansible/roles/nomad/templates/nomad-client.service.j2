[Unit]
Description=Nomad client
Wants=network-online.target
After=network-online.target

[Service]
ExecStart=/bin/sh -c "/usr/bin/nomad agent -config /etc/nomad/nomad-client.hcl -bind={{ ansible_default_ipv4.address }}"
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
